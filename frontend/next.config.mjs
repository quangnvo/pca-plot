/** @type {import('next').NextConfig} */
const nextConfig = {
    // Put "export" in the "output" to make the folder "out" after running "next build"
    output: 'export',
};

export default nextConfig;
