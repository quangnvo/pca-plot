/** @type {import('next').NextConfig} */
const nextConfig = {
    // Put "export" in the "output" to generate the folder "out" after running "npm run build"
    output: 'export',
};

export default nextConfig;
